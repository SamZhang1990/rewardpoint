server:
  port: 12000
  
spring:
  application:
    name: rewardpoint-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/sam_git/rewardpoint-config.git
          username: 
          password: 
  rabbitmq: 
    host: 192.168.43.110
management: 
  endpoints:  
    web:  
      exposure: 
        include:  bus-refresh
          
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone:  http://localhost:6868/eureka/
#  instance:
#    prefer-ip-address: true


    